export enum ToastTypes {
  successToast = 'successToast',
  errorToast = 'errorToast',
}

export enum ErrorCodes {
  ERR_NETWORK = 'ERR_NETWORK',
  ERR_BAD_REQUEST = 'ERR_BAD_REQUEST',
}

export enum MovieRateTypes {
  R = 'R',
  ALL = 'All',
}
