import {Text, View} from 'react-native';
import React from 'react';
import {utilsScreenStyles} from '../../utils/styles';

const Home = () => {
  return (
    <View style={utilsScreenStyles.container}>
      <Text>Home</Text>
    </View>
  );
};

export default Home;
