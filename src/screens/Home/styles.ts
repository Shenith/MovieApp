import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 10,
  },
  refreshContainer: {backgroundColor: 'transparent'},
  activityIndicator: {paddingTop: 10},
});
